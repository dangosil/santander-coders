//  * OPERADOR TERNÁRIO

const idade = 16;

// idade >= 18 ? console.log('Maior de idade') : console.log('Menor de idade');

const msg = idade >= 18 ? "Maior de idade" : "Menor de idade";
const msg2 = idade >= 18 ? "Maior de idade" : idade >= 16 ? "Já pode votar" : "Não pode votar";

console.log(msg);
console.log(msg2);

if(undefined) {

}